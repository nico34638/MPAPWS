{% extends 'base.html.twig' %}

{% block title %}Rechercje{% endblock %}

{% block body %}


    <div class="container" style="margin-top: 120px">
        <div class="row" class="form-inline">
            <h3>Recherche </h3>
            <form method="post" class="form-inline d-flex justify-content-center md-form form-sm mt-0">
                {{ form_start(form) }}
                {{ form_errors(form) }}

                {{ form_widget(form.content, {'attr': {'class': 'form-control form-control-sm ml-3 w-75', 'placeholder': 'Votre prénom'}}) }}
                <button type="submit"><i class="fas fa-search" aria-hidden="true"></i></button>
                {{ form_end(form) }}
            </form>
        </div>
    </div>

{% endblock %}
